<h1>Index</h1>

<% flash.each do |name, msg| %>
  <%= msg %>
<% end %>
<br>

<% @users.each do |u| %>
  Name: <%= u.name %> (<%= link_to('Add friend', friendships_path(friend_id: u), method: :post) %>)<br>
  Email: <%= u.email %><br>
  Posts: <%= link_to('View posts', user_posts_path(u)) %><br>
  <br>
<% end %>

<%= render('layouts/links') %>